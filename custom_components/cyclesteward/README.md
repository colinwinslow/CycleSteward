# Home Assistant adapter placeholder

The Home Assistant custom component is intentionally not implemented in the seed
repo. The first implementation slice should prove the charge modeling core in
`src/cyclesteward/` with fixtures. After that, this directory can wrap the core
model with Home Assistant config flow, entities, services, and persistent
profile storage.
